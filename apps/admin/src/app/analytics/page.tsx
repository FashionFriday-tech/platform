import React from 'react';

import { AnalyticsView } from '../../features/analytics';

export const metadata = {
  title: 'Advanced Analytics | Fashion Friday',
};

export default function AnalyticsPage() {
  return <AnalyticsView />;
}
