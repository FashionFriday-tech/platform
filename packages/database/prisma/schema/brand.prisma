model Brand {
  id         String   @id @default(uuid())
  name       String
  slug       String   @unique
  logo       String
  color      String
  categories String[]

  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@map("brands")
}
