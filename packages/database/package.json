{
  "name": "@ff/database",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "build-schema": "prisma-import --schema prisma/schema.prisma --output prisma/schema.prisma",
    "generate": "npm run build-schema && prisma generate",
    "migrate": "npm run build-schema && prisma migrate dev"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "prisma-import": "^1.0.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}
